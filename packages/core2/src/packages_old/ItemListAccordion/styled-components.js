import { styled } from "../../packages/base/styles";

export const itemListContainerStyles = ({ $theme }) => ``;
export const ItemListContainerStyled = styled("div", itemListContainerStyles);

export const toggleContainerStyles = ({ $theme }) => ``;
export const ToggleContainerStyled = styled("div", toggleContainerStyles);
